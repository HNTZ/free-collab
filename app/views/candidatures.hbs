<div class="container col-10 p-1">
<h1>Mes candidatures</h1>
{{#if applications}}
    {{#each applications}}
    <div class="row mb-3">
        <div class="container rounded p-3 d-flex border justify-content-between">
            <p>
                <span class="font-weight-bold">{{project}}</span>
                <br>
                <span class="badge badge-pill {{#equals status "En attente"}}badge-warning{{else}}{{#equals status "Accepté"}}badge-success{{else}}badge-danger{{/equals}}{{/equals}}">{{status}}</span>
            </p>
            <a href="/projets/{{project_id}}" class="btn p-2 btn-danger mx-0 my-auto">Voir le projet</a>
        </div>
    </div>
    {{/each}}
{{else}}
<p>Vous n'avez fait aucune candidature.</p>
<a class="btn btn-primary" href="/recherche">Trouvez des projets qui vous intéressent !</a>
{{/if}}
</div>
