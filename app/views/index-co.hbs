<div class="container">
    <br>
    <h1 class="d-flex flex-row justify-content-center">Bienvenue {{user.firstname}} {{user.lastname}} !</h1>
    <br>
    <div class="row">
        <div class="col d-flex">
            <a class="btn btn-primary mx-auto" href="/projets/nouveau"><i class="fa fa-plus text-white pr-2"></i>Créer un projet</a>
        </div>
    </div>
    <br>
    <div class="container">
        <div class="row">
            <div class="col-xs-6 col-sm-6 col-md-6">
                <h4 class="d-flex flex-row justify-content-center">Mes projets</h4>
                <br>
                {{#if projects}}
                {{#each projects}}
                <a href="/projets/{{_id}}" class="list-group-item list-group-item-action">{{name}}<span
                        class="badge badge-pill badge-secondary ml-2">{{category}}</span></a>
                {{/each}}
                <br>
                <a href="/projets" class="mt-2 d-flex flex-row justify-content-center">Voir tous mes projets</a>
                {{else}}
                
                <p>Vous n'êtes membre d'aucun projet actuellement.</p>
                {{/if}}
            </div>
            
            <div class="col-xs-6 col-sm-6 col-md-6">
                <h4 class="d-flex flex-row justify-content-center">Mes Candidatures</h4>
                <br>
                {{#if applications}}
                {{#each applications}}
                <p class="list-group-item list-group-item-action"><a href="/projets/{{project_id}}">{{project}}</a><span
                        class="badge badge-pill {{#equals status "En attente"}}badge-warning{{else}}{{#equals status "Accepté"}}badge-success{{else}}badge-danger{{/equals}}{{/equals}} ml-2">{{status}}</span>
                </p>
                {{/each}}
                <br>
                <a href="/candidatures" class="mt-2 d-flex flex-row justify-content-center">Voir toutes mes candidatures</a>
                {{else}}
                <p>Vous n'avez fait aucune candidature.</p>
                {{/if}}
            </div>
        </div>
    </div>
    <div class="container" style="padding: 6%;">
        <div class="d-flex justify-content-center">
            <div class="d-flex flex-column">
                <form action="/recherche" class="form-group">
                    <div class="justify-content-center row">
                        <input class="form-control px-4 py-2" type="search" style="height: 44px;" name="query" id="query"
                            placeholder="🔎 Rechercher...">
                    </div>
                    <div class="d-flex flex-row justify-content-center"style="margin: 5% 0 5% 0;">
                        <a href="/recherche" class="btn btn-primary">Tous les projets</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>